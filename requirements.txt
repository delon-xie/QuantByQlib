# QuantByQlib 依赖清单
# Python 3.10.x

# ── UI 框架 ────────────────────────────────────────────────────
PyQt6>=6.7.0
pyqtgraph>=0.13.0

# ── Qlib 生态 ──────────────────────────────────────────────────
pyqlib>=0.9.6
mlflow >= 3.12
joblib == 1.2.0
loky >= 3.4

# ── OpenBB Platform（数据统一接入层）──────────────────────────
openbb>=4.0.0
openbb-yfinance>=1.0.0
openbb-alpha-vantage>=1.0.0
openbb-fmp>=1.0.0
finnhub-python>=1.0.0
openbb-cboe>=1.0.0

# ── 情绪分析 ──────────────────────────────────────────────────
nltk>=3.8.0
transformers>=4.40.0

# ── 深度学习 ──────────────────────────────────────────────────
# 老机器支持
torch>=2.2.0
lightgbm>=4.3.0
catboost>=1.2.10
xgboost>=3.2.0

# ── RD-Agent Docker 集成 ──────────────────────────────────────
docker>=7.0.0

# ── 数据处理 ──────────────────────────────────────────────────
pandas>=2.2.0

# numpy 2.0+ 不兼容
numpy==1.26.4

# ── 图表 ──────────────────────────────────────────────────────
mplfinance>=0.12.10b0

# ── 长桥行情 ──────────────────────────────────────────────────
longport

# ── HMM 政体识别（F3）────────────────────────────────────────
hmmlearn>=0.3.0

# ── 工具库 ────────────────────────────────────────────────────
pyyaml>=6.0.0
loguru>=0.7.0
python-dotenv>=1.0.0
requests>=2.31.0

# ── 可选依赖（仅使用「因子发现」功能时需要）Claude Code──────────────────────
anthropic>=0.97.0
openai>=2.32.0
tqdm>=4.67.3
duckdb>=1.5.2
pytest 
pytest-cov
python-binance
PyQt6-WebEngine 
markdown 
Pygments